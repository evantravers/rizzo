:ruby

  properties = properties || {}

  data_props = {
    size: { mapping: properties[:size] || "sponsor-tile" },
    extension: properties[:extension] || nil,
    targeting: properties[:targeting] || nil,
    context: properties[:context] || nil
  }

  id = properties[:id] || "js-ad-sponsorTile-#{Time.now.to_i}"

.adunit.adunit--sponsortile{ id: id, data: data_props }
